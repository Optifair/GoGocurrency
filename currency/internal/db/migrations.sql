CREATE TABLE IF NOT EXISTS rates (
                                     date DATE PRIMARY KEY,
                                     usd FLOAT NOT NULL,
                                     eur FLOAT NOT NULL
);
